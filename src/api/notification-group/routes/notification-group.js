'use strict';

/**
 * notification-group router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notification-group.notification-group');
